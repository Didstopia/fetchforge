/* eslint-disable no-unused-expressions */
/* global describe, it */

const chai = require('chai')
const expect = chai.expect

describe('App', () => {
  describe('CLI', () => {
    it('TODO: Test CLI stuff', () => {
      expect(true).be.true
    })
  })
})
